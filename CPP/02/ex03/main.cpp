#include "Point.hpp"

bool bsp(Point const a, Point const b, Point const c, Point const point);

int main()
{
	Point a(2, 4);
	Point b(3, 7);
	Point c(1, 9);
	Point p(2000, 6000);
	//std::cout << bsp(a, b, c, p) << std::endl;
}
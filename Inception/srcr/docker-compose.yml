services:
    nginx:
        build: ./requirements/nginx/
        ports:
            - "80:80"
        volumes:
            - website:/usr/share/nginx/html
volumes:
    website:
        driver: local
        driver_opts:
            type: none
            o: bind
            device: ~/data/website
